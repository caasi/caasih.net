(function(jaws) {
  var gameState = {
    setup: function() {
      console.log("game initialized");
    },
    update: function() {},
    draw: function() {}
  };

  jaws.start(gameState);
}(jaws));
